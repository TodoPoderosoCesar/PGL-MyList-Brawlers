#  BRAWLERS.DATA.TSX

Este fichero super sencillo solo tiene dentro un array con la informacion de varios brawlers para crear la lista. 
Si quieres abrir el fichero pincha [aquí](../data/brawlers-data.tsx).

### IMPORTS:

Este fichero carga un fichero [BrawlerItem](../types/BrawlerItem.tsx) que define los tipos que ha de tener un brawler.

### FUNCION APP:

Este fichero no es mas que una constante de información que desplegar en la lista del [App.tsx](../App.tsx).

### Autocrítica:

Deberia haber implementado dentro de cada brawler un id, huebiera ayudado mucho para eliminar o desplegar los brawlers.