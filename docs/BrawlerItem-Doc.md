#  BRAWLERITEM.TSX

Este fichero Define de como ha de ser 100% un objeto Brawler.
Si quieres abrir el fichero pincha [aquí](../types/BrawlerItem.tsx).

### FUNCION APP:

Este fichero solo consta de un export de un types con los tipos de parametros o atributos definidos. Los atributos son:
- image con tipo any
- name con tipo string
- rarity con tipo string
- credits con tipo number


### Autocrítica:

Deberia haber definido tambien un atributo id.